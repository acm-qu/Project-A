/**
 * Extract course codes from the student's pasted content.
 * @param content The content string to extract course codes from.
 * @returns An array of course codes found in the content.
 */
export function getCourseCodes(content: string): string[] {
  return [""]
}